# Meta API Credentials
# ⚠️ NÃO COMMITAR .env REAL — use .env.example como template
# Copie para .env e preencha com valores reais

BUSINESS_ACCOUNT_ID=2274467833382298
PAGE_ID=[gerado por meta_auth.py]
PAGE_ACCESS_TOKEN=[gerado por meta_auth.py]
PHONE_NUMBER_ID=[gerado por meta_auth.py]
META_APP_SECRET=[após verificação Meta]
META_VERIFY_TOKEN=[gerado aleatório]
ACCESS_TOKEN=[seu token permanente de usuário do sistema]

# Anthropic (CRÍTICO para ProteOS responder inteligentemente)
ANTHROPIC_API_KEY=sk-ant-[sua chave em console.anthropic.com/settings/keys]

# Supabase (já existente)
SUPABASE_URL=https://agebsmjsjrmazbozphnh.supabase.co
SUPABASE_SERVICE_KEY=[chave da sessão 29/06]

# Webhook Meta (configure no painel)
WEBHOOK_PATH=/api/v1/meta/webhook
WEBHOOK_VERIFY_TOKEN=[igual a META_VERIFY_TOKEN acima]
