# business-agent/requirements.txt
# Dependências reais do webhook Meta + ProteOS + bridge de voz (nao existia
# arquivo antes; instalacao era manual via META_INTEGRATION_COMPLETE.md).

fastapi
uvicorn
httpx
pydantic
python-dotenv
python-multipart  # voice_proxy.py usa Form(...) — FastAPI exige isso

# ProteOS (Anthropic) — SDK oficial, com prompt caching
anthropic>=0.40.0

# Persistência / leads / conversas
supabase

# instagram_poster.py
pillow
cairosvg

# agents_graph.py — orquestração multiagente com loop de tool-calling
# Testado localmente em 13/08/2026 com langgraph 1.2.11 + langchain-anthropic 1.5.6.
langgraph>=0.2.0
langchain-anthropic>=0.3.0

# test_agents_graph.py
pytest
