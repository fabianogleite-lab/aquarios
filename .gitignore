node_modules/
__pycache__/
*.pyc
.env
.env.local
.env.production
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
*.log
android/
ios/
.idea/
*.iml
.DS_Store
Thumbs.db
.claude/settings.local.json
.claude/projects/

# Handoffs/memória de sessão — nunca versionar (podem conter segredos em texto puro)
memory/
mobile/memory/

# SandeirOS — dados do Motor de Humanização: NUNCA versionar (heurística proprietária)
backend/sandeiros/data/hl1_*.json
backend/sandeiros/data/hl2_*.json
backend/sandeiros/data/hl3_*.json
backend/sandeiros/data/hl4_*.json
# ... e as cópias dentro de qualquer pasta de versão (mvp0/mvp1/mvp2…)
mvp*/**/hl[1-4]_*.json

# Infra docs — contêm IP/topologia/logins de contas; repo é PÚBLICO.
# Mantidos local-only (igual memory/). Ver feedback_sites_pro_template.
INFRA_HARDWARE_MAP.md
INFRA_FREE_TIER_EXPIRY.md

# Supabase CLI — scratch local; expõe project-ref + host do pooler (repo é PÚBLICO)
# Cobre raiz e as cópias em mvp0/ mvp1/ (hardening 22/Jun, item 6)
**/supabase/.temp/

# Terraform — state/vars podem conter segredos; nunca versionar
infra/**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
